% Real-data frequency sweep: log-log G'/G'' vs omega (SI units)
% Arg 1: \addplot commands (list G' before G'' per system for correct rheology cycle)
% Arg 2: extra pgfplots axis options (override defaults)
\newcommand{\mdplotGfreq}[2]{%
  \begin{tikzpicture}[trim axis left, trim axis right]
    \begin{loglogaxis}[
      mdplots-linear,
      cycle list name=mdplots-rheology,
      every axis plot/.append style={mark size=1.5pt},
      legend pos=north west,
      xlabel={\(\omega\) [rad/s]},
      ylabel={\(G', G''\) [Pa]},
      #2
    ]
      #1
    \end{loglogaxis}
  \end{tikzpicture}%
}
